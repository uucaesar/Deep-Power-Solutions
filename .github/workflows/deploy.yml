name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    # REMOVED: - name: Change directory (This doesn't work!)

    - name: 🔨 Install Dependencies
      run: npm install
      working-directory: ./src   # <--- FIX 1: Tell npm where to look

    - name: 🏗️ Build Project
      run: npm run build
      working-directory: ./src   # <--- FIX 2: Build inside the folder too

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        # FIX 3: Your dist folder is now inside src too!
        local-dir: ./src/dist/